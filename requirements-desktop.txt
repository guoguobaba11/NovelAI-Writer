# NovelAI Writer 桌面应用依赖
# 核心
openai>=1.30.0
anthropic>=0.30.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# 桌面应用
pywebview>=5.0
pymsgbox>=1.0        # 错误对话框（pywebview 依赖）
pyinstaller>=6.0     # 打包 .exe 用
